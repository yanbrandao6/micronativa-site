import {ServicePageTemplate} from "@/components/ServicePageTemplate";import {serviceBySlug} from "@/data/services";import {pageMetadata} from "@/lib/metadata";export const metadata=pageMetadata("Controle de Acesso","Tags, biometria, fechaduras eletrônicas, vídeo porteiros e gestão de entradas.","/controle-acesso");export default function Page(){return <ServicePageTemplate service={serviceBySlug["controle-acesso"]}/>}

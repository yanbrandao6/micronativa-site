"use client";import {Search} from "lucide-react";import {useMemo,useState} from "react";import {ProjectCard} from "@/components/ProjectCard";import {projects} from "@/data/projects";import {trackEvent} from "@/lib/analytics";import {cn} from "@/lib/utils";
const filters=[["todos","Todos"],["energia-solar","Energia Solar"],["cftv-seguranca","CFTV"],["automacao-portoes","Automação de Portões"],["controle-acesso","Controle de Acesso"],["projeto-integrado","Projetos Integrados"]];
export function ProjectsExplorer(){const [filter,setFilter]=useState("todos");const [query,setQuery]=useState("");const items=useMemo(()=>projects.filter(p=>p.published&&(filter==="todos"||p.category===filter)&&(p.title+" "+p.summary+" "+p.propertyType).toLowerCase().includes(query.toLowerCase())),[filter,query]);return <><div className="mt-10 flex flex-col gap-4 rounded-3xl border bg-white p-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap gap-2" role="group" aria-label="Filtrar projetos">{filters.map(([value,label])=><button key={value} type="button" aria-pressed={filter===value} onClick={()=>{setFilter(value);trackEvent("project_filter_used",{category:value});}} className={cn("min-h-11 rounded-full px-4 text-sm font-bold",filter===value?"bg-forest text-white":"bg-warm text-navy hover:bg-forest-pale")}>{label}</button>)}</div><label className="relative block min-w-64"><span className="sr-only">Buscar projetos</span><Search className="absolute left-4 top-1/2 size-5 -translate-y-1/2 text-muted"/><input className="input pl-11" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar projeto"/></label></div><p className="mt-6 text-sm font-semibold text-muted" aria-live="polite">{items.length} estudo(s) encontrado(s)</p><div className="mt-6 grid gap-6 sm:grid-cols-2 xl:grid-cols-3">{items.map(p=><ProjectCard key={p.id} project={p}/>)}</div>{!items.length&&<div className="mt-6 rounded-3xl bg-white p-10 text-center text-muted">Nenhum projeto corresponde aos filtros.</div>}</>}

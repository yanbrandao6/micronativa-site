import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { WhatsAppButton } from "@/components/WhatsAppButton";
import type { WhatsAppContext } from "@/lib/whatsapp";
export function CTASection({ title = "Seu imóvel pode ser mais econômico, seguro e inteligente.", text = "Converse com nossa equipe e descubra quais soluções são mais adequadas para o seu projeto.", context = "home", service }: { title?: string; text?: string; context?: WhatsAppContext; service?: any }) {
  return <section className="section-pad"><div className="container-site"><div className="relative overflow-hidden rounded-4xl bg-navy px-6 py-14 text-white sm:px-12 lg:px-16 lg:py-20"><div className="absolute -right-20 -top-20 size-64 rounded-full border-[40px] border-solar/20"/><div className="relative max-w-3xl"><p className="eyebrow !text-solar">Próximo passo</p><h2 className="mt-4 text-3xl font-bold tracking-tight sm:text-5xl">{title}</h2><p className="mt-5 text-lg leading-8 text-white/75">{text}</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><WhatsAppButton context={context} service={service} label="Falar com um especialista" className="bg-solar text-ink hover:bg-white" /><Link href={"/orcamento" + (service ? "?service=" + service : "")} className="btn border border-white/20 bg-white/10 text-white hover:bg-white hover:text-navy">Enviar solicitação <ArrowRight className="size-4" /></Link></div></div></div></div></section>;
}
